import React from 'react'

function Fragment() {
    return (
        <React.Fragment>
            <h1>Freagment</h1>
            <p>Fragment is use to remove extra div. When we are using multi line return.</p>
            <p>We can pass key props in React.Fragment tag.</p>
        </React.Fragment>
    )
}

export default Fragment